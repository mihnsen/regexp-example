/**
 * linkify
 */
describe('linkify text', function() {

  it('should defined', function() {
    expect(linkify).toBeDefined();
  });

  it('should have ', function() {
    expect(linkify).toBeDefined();
  })
});
